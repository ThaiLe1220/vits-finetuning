brew install pyenv-virtualenv
pyenv install 3.8.12
pyenv virtualenv 3.8.12 vits-env
exec "$SHELL"
pyenv activate vits-env
pip install torch torchvision
pip install -r requirements.txt

pyenv deactivate vits-env
